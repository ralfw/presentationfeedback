<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="#">Presentation feedback</a>
		</div>
		<div id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav navbar-right">
				<li @HighlightActive("Home")><a href="@Url.Action("Index", "Home")">Home</a></li>
				<li @HighlightActive("Conference")><a href="@Url.Action("List", "Conference")">Conferences</a></li>
				<li @HighlightActive("About")><a href="@Url.Action("About", "Home")">About</a></li>
				<li @HighlightActive("Contact")><a href="@Url.Action("Contact", "Home")">Contact</a></li>
			</ul>
		</div>
	</div>
</nav>

@helper HighlightActive(string item)
{
	if (@ViewBag.SelectedMenuItem == item)
	{
		<text>class="active"</text>
	}
}