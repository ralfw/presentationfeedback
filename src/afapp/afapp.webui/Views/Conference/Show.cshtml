<link rel="stylesheet" href="/Content/css/conference/show.css">

@using System.Globalization
@using Contract.data
@using MongoDB.Driver.Linq
@model Contract.data.SessionOverview

<div class="jumbotron">
	<h1>Conference: @Model.ConfId - @Model.ConfTitle</h1>
</div>
<div class="sessionDiv">
	<h2>Active sessions</h2>
	<div class="container">
		<row>
			<div class="col-lg-8">
				@if (Model.ActiveSessions.Length > 0)
				{
					@RenderSessions(Model.ActiveSessions, true)
				}
				else
				{
					<div class="bs-callout">
						<h4>No active sessions</h4>
					</div>
				}
			</div>
		</row>
	</div>
</div>

<div class="sessionDiv">
	<h2>Inactive sessions</h2>
	<div class="container">
		<row>
			<div class="col-lg-8">
				@if (Model.InactiveSessions.Length > 0)
				{
					@RenderSessions(Model.InactiveSessions, false)
				}
				else
				{
					<div class="bs-callout">
						<h4>No inactive sessions</h4>
					</div>
				}
			</div>
		</row>
	</div>
</div>

@helper RenderSessions(IEnumerable<Session> sessions, bool showLink)
{
	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Title</th>
				<th>Start</th>
				<th>End</th>
				<th>Speaker</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var row in sessions)
			{
				<tr>
					<td>@row.Id</td>
					<td>
						@if (showLink)
						{
							<a href="@Url.Action("Show", "Session", new {id = row.Id})">@row.Title</a>
						}
						else
						{
							@row.Title
						}
					</td>
					<td>@String.Format("{0:dd-MM-yyyy HH:mm}", row.Start)</td>
					<td>@String.Format("{0:dd-MM-yyyy HH:mm}", row.End)</td>
					<td>@row.SpeakerName</td>
				</tr>
			}
		</tbody>
	</table>
}
